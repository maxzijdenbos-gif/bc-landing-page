import { describe, expect, it } from 'vitest';

describe('Vitest unit project', () => {
  it('runs with npm run test (add real tests as src/**/*.test.ts(x))', () => {
    expect(1 + 1).toBe(2);
  });
});
