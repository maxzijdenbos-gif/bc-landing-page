import GlobalToLocalContentTemplate from 'components/templates/amplience/dashboards/global-to-local/global-to-local-content';

const GlobalToLocalContent = () => {
  return <GlobalToLocalContentTemplate />;
};

export default GlobalToLocalContent;
