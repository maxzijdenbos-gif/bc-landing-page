import { LeadParagraphProps } from './lead-paragraph';

const defaultStub: LeadParagraphProps = {
  text: 'LeadParagraph',
};

export default <Record<string, LeadParagraphProps>>{
  default: defaultStub,
};
