import { EmbeddedVideoProps } from './embedded-video';

const defaultStub: EmbeddedVideoProps = {
  wistiaId: 'haja5rt840',
};

export default <Record<string, EmbeddedVideoProps>>{
  default: defaultStub,
};
